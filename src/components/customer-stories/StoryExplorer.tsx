import { useState } from "react";
import { stories } from "./customerStories";
export default function StoryExplorer() { const [activeId, setActiveId] = useState(stories[0].id); const story = stories.find((item) => item.id === activeId) ?? stories[0]; return <section id="stories" className="cs-stories cs-section-pad"><div className="cs-shell"><div className="cs-section-head"><span className="cs-eyebrow">Customer stories by solution</span><h2>Start with the work<br />you want to improve.</h2></div><div className="cs-story-explorer"><aside className="cs-story-nav" aria-label="Explore by solution"><div className="cs-story-nav-label">Explore by solution</div>{stories.map((item) => <button type="button" key={item.id} className={`cs-story-tab ${item.id === activeId ? "active" : ""}`} onClick={() => setActiveId(item.id)}><small>{item.industry}</small><strong>{item.title}</strong><span>{item.navMetric}</span></button>)}</aside><article className="cs-story-content" aria-live="polite"><div className="cs-story-copy"><span className="cs-story-pill">{story.industry}</span><h3>{story.title}</h3><div className="cs-copy-block"><h4>Challenge</h4><p>{story.challenge}</p></div><div className="cs-copy-block"><h4>Solution</h4><p>{story.solution}</p></div><blockquote className="cs-story-quote"><p>“{story.quote}”</p><cite>— {story.quoteBy}</cite></blockquote></div><aside className="cs-results-panel"><span className="cs-results-label">Results</span><div className="cs-results-grid">{story.metrics.map((metric) => <div className="cs-result-card" key={metric.label}><strong>{metric.value}</strong><span>{metric.label}</span></div>)}</div></aside></article></div></div></section>; }